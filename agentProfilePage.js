import {properties} from "./mockAPI";

const renderAgentProperties = () => {
    const propertyRows = properties.map(property => {
       return `<li>
            <div class = "card">
        </li>`
    })
}